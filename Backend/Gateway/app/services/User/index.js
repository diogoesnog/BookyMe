const Booking = require('./bookings');
const Favorite = require('./favorites');
const Review = require('./reviews');
const Store = require('./store');
const User = require('./users');

module.exports = {
    Booking, Favorite, Review, Store, User
}